/**
 * Preview route for rendering components in isolation.
 * Used by Back Office to preview themes and components.
 */
export default function PreviewPage() {
  // TODO: Accept component name and props via query params
  // TODO: Resolve and render component dynamically
  return (
    <div>
      <p>Preview Mode</p>
    </div>
  );
}
